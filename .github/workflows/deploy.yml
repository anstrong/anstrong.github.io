name: GitHub Pages Deployment

on:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build React app
        run: npm run build
      - name: Update gh-pages branch
        run: npm run deploy
